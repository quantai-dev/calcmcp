[project]
name = "calcmcp"
version = "0.1.0"
description = "MCP server for LibreOffice Calc via UNO bridge"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.0.0",
]

[project.scripts]
calcmcp = "cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
